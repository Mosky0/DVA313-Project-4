FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci --silent

COPY . .

ENV VITE_API_BASE_URL=http://localhost:8000/api

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "--port", "3000"]